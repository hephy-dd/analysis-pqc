{% set flute=['i_surf', 'meander_poly', 't_line_n', 't_line_pstop4', 't_line_pstop2', 'v_bd'] %}
{% set extraheader=' & GCD & Poly-R & \multicolumn{3}{ c|}{Line thickness} & breakdown\\\\' %}



{% extends 'latex.tex' %}

{# for some reason the relative path does not work, so we have to make do with the links #}
